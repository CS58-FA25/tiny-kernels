# Final Submission
- Tested on all user-space programs in the `user` directory

# Team
- Isabella Fusari
- Ahmed Al Sunbati

## Docker (if not on thayer machine)
### Build
docker buildx build --platform linux/amd64 .docker -t yalnix

### Run
./.docker/run.sh

checking